[![Build Status](https://travis-ci.org/dritter-hd/stringsearch.svg?branch=master)](https://travis-ci.org/dritter-hd/stringsearch)

# stringsearch

Build using
```
cmake -B build .
cmake --build build
```
and run
```
./build/benchmark
```
